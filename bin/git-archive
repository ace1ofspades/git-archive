#!/usr/bin/env bash
set -e

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

if [[ -n "$GIT_ARCHIVE_LIB" ]]; then
  LIB_DIR="$GIT_ARCHIVE_LIB"

elif [[ -d "$SCRIPT_DIR/../lib" ]]; then
  # local repo usage
  LIB_DIR="$SCRIPT_DIR/../lib"

elif [[ -d "$SCRIPT_DIR/../libexec" ]]; then
  # Homebrew Cellar direct layout
  LIB_DIR="$SCRIPT_DIR/../libexec"

elif [[ -d "/opt/homebrew/opt/git-archive/libexec" ]]; then
  # Homebrew opt prefix (stable & recommended)
  LIB_DIR="/opt/homebrew/opt/git-archive/libexec"

else
  echo "❌ Cannot locate git-archive lib directory" >&2
  exit 1
fi

source "$LIB_DIR/common.sh"
source "$LIB_DIR/archive.sh"
source "$LIB_DIR/extract.sh"

if [[ "$1" == "--version" ]]; then
  echo "git-archive 0.2.1"
  exit 0
fi

CMD="archive"
if [[ "$1" == "extract" ]]; then
  CMD="extract"
  shift
fi

case "$CMD" in
  archive) run_archive "$@" ;;
  extract) run_extract "$@" ;;
  *) usage; exit 64 ;;
esac
